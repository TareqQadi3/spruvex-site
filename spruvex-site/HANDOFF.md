# HANDOFF — SpruVex R Marketing Site

مرجع تقني شامل لهذا المشروع: ماذا بُني، كيف يعمل، كيف يرتبط بـ spruvex-r، وأين
تقف الأمور الآن. اقرأ هذا الملف كاملًا قبل أي تعديل — خصوصًا قسم **"المشاكل
غير المحلولة"** في الأسفل.

---

## 1) ما هو هذا المشروع

موقع تسويقي (Next.js 16، App Router) لمنتج **SpruVex R** — نظام SaaS لإدارة
المطاعم/نقاط البيع في السعودية (متعدد المستأجرين). هذا المستودع **ليس** المنتج
نفسه؛ المنتج الفعلي (لوحة تحكم + API) في مستودع منفصل اسمه **`spruvex-r`**.

مسؤولية هذا الموقع:
- صفحة رئيسية تسويقية + صفحة أسعار.
- نموذج "تجربة مجانية 14 يومًا" (`/contact`) — **يُنشئ حسابًا حقيقيًا فعليًا**
  في spruvex-r عبر API عام (وليس مجرد نموذج تواصل).
- تدفّق اشتراك مدفوع عبر تحويل بنكي يدوي (`/pay/[plan]`) بمراجعة إدارية.
- لوحة إدارة داخلية بسيطة (`/admin`) لمراجعة طلبات التجربة والدفع.

---

## 2) المكدس التقني

- **Next.js 16** (App Router, Turbopack), **React 19**, **TypeScript**, **Tailwind v4**
- **Zod** للتحقق من المدخلات (`src/lib/validation.ts`)
- **better-sqlite3** — قاعدة بيانات محلية لهذا الموقع فقط (تفاصيل §5)
- **Resend** (`resend` npm package) — بريد ترحيب/تنبيه إدارة (§6)
- **framer-motion** للحركات، **lucide-react** للأيقونات
- **jose** لجلسة إدارة JWT (`/admin`)
- النشر مخطَّط له على **Render** (Node Web Service + Persistent Disk) — انظر
  `render.yaml` بجذر المستودع (خارج مجلد `spruvex-site/`). **ليس** Vercel،
  لأن SQLite يحتاج قرصًا دائمًا لا يوفّره الاستضافة عديمة الحالة (serverless).

---

## 3) بنية المشروع (أهم الملفات)

```
spruvex-site/
├── src/
│   ├── app/
│   │   ├── page.tsx                    الرئيسية (Hero, Features, TrustBar, Pricing teaser, NationalDayBanner)
│   │   ├── pricing/page.tsx             صفحة الأسعار الكاملة
│   │   ├── contact/page.tsx             صفحة "ابدأ تجربة مجانية" (فورم أولًا، صندوق تواصل أسفلها)
│   │   ├── pay/[plan]/page.tsx          صفحة إتمام الدفع بالتحويل البنكي
│   │   ├── admin/                       لوحة الإدارة (محمية بكلمة مرور + جلسة JWT)
│   │   ├── layout.tsx                   Header + Footer + WhatsAppButton العائم
│   │   └── api/
│   │       ├── trial-signup/route.ts            POST — التسجيل الفعلي (يستدعي spruvex-r)
│   │       ├── trial-signup/verify/route.ts     POST — وسيط للتحقق من رمز OTP
│   │       ├── trial-signup/resend-otp/route.ts POST — وسيط لإعادة إرسال الرمز
│   │       ├── payment-submission/route.ts      POST — إرسال بيانات تحويل بنكي
│   │       └── admin/...                        تسجيل دخول/خروج الإدارة، عرض/مراجعة الطلبات
│   ├── components/
│   │   ├── forms/TrialForm.tsx          فورم التجربة المجانية (Client Component، آلة حالات)
│   │   ├── forms/BankTransferForm.tsx   فورم الدفع بالتحويل (يشمل كود خصم)
│   │   ├── layout/WhatsAppButton.tsx    زر واتساب عائم ثابت
│   │   └── ...
│   └── lib/
│       ├── constants.ts                 PLANS, BUSINESS_TYPES, NATIONAL_DAY_PROMO, BRAND, ...
│       ├── validation.ts                كل Zod schemas (هاتف سعودي، بريد، كلمة مرور، ...)
│       ├── db.ts                        اتصال SQLite + إنشاء الجداول (lazy، انظر §5)
│       ├── spruvexR.ts                  ⚠️ العميل الوحيد الذي يستدعي spruvex-r (server-only)
│       ├── email.ts                     عميل Resend (server-only)
│       ├── csrf.ts / rateLimit.ts       حماية CSRF (double-submit cookie) + rate limiting بالذاكرة
│       ├── session.ts                   جلسة إدارة (JWT بكوكي httpOnly)
│       └── repositories/
│           ├── trialSignups.ts          CRUD جدول trial_signups
│           └── paymentSubmissions.ts    CRUD جدول payment_submissions + uploaded_files
├── render.yaml                          (بجذر المستودع، وليس هنا) — Render Blueprint
├── .env.example                         كل متغيرات البيئة موثّقة هنا
└── README.md                            توثيق تشغيلي أقصر (هذا الملف أشمل)
```

---

## 4) الربط مع spruvex-r — التفصيل الكامل

### 4.1 المبدأ المعماري

كل استدعاء لـ spruvex-r يمر **حصرًا** عبر `src/lib/spruvexR.ts` — ملف
server-only لا يُستورد أبدًا من أي مكوّن `"use client"`. لا صفحة ولا مكوّن
عميل يعرف عنوان spruvex-r أو مفتاحه؛ المتصفح يتحدث فقط مع Route Handlers هذا
الموقع (`/api/trial-signup*`)، وهي التي تتحدث مع spruvex-r من جانب السيرفر.

**لا منطق موازٍ**: كل ما يحدث بجانب spruvex-r (إنشاء Tenant، OTP، كلمة المرور)
يعيد استخدام نفس الآليات الحقيقية التي يستخدمها أي مستخدم يسجّل ذاتيًا هناك —
لا نسخة "مبسّطة" منفصلة.

### 4.2 نقاط النهاية المستخدمة (كلها تحت `{SPRUVEX_R_API_URL}/api/v1`)

| المسار | من يستدعيه هنا | يحتاج مفتاح API | الوصف |
|---|---|---|---|
| `POST /public/trial-signup` | `createSpruvexRTrial()` | نعم — ترويسة `x-spruvex-site-key` | ينشئ Tenant تجريبي حقيقي (14 يوم) |
| `POST /auth/register/verify` | `verifySpruvexRTrialOtp()` | لا (مسار عام أصلًا) | يتحقق من رمز OTP المكوّن من 6 أرقام |
| `POST /auth/register/resend-otp` | `resendSpruvexRTrialOtp()` | لا | يعيد إرسال رمز جديد (يرجع 200 دائمًا) |
| `POST /auth/login` | *(غير مُستدعى من هذا الموقع بعد)* | لا | تسجيل دخول لاحق بالبريد+كلمة المرور — يُستخدم مباشرة من لوحة spruvex-r نفسها |

### 4.3 تدفّق التسجيل الكامل (خطوة بخطوة)

1. المستخدم يعبّئ `/contact`: اسم النشاط، نوع النشاط، جوال، بريد، **كلمة مرور
   يختارها بنفسه** + تأكيدها.
2. `POST /api/trial-signup` (هذا الموقع):
   - يتحقق من Zod schema (`trialSignupSchema`) + CSRF + rate limit (5/10 دقائق لكل IP).
   - **فحص تكرار محلي**: لو البريد له سجل `provisioned` سابق محليًا → يرجع
     `{alreadyRegistered: true}` فورًا بدون استدعاء spruvex-r.
   - يُنشئ سجلًا محليًا بجدول `trial_signups` (بدون كلمة المرور أبدًا — لا تُخزَّن هنا).
   - يستدعي `createSpruvexRTrial({restaurantName, phone, email, password, businessType})`
     — مع إعادة محاولة تلقائية (حتى محاولتين إضافيتين) لفشل الشبكة/5xx العابر
     فقط (وليس 401/409).
   - **نجاح (201)** → يُعلَّم السجل `provisioned` (مع `tenant_id`/`dashboard_url`)،
     يرسل بريد تنبيه للإدارة، ويرجع للمتصفح `{provisioned: true, email, dashboardUrl}`.
   - **409 (جوال/بريد مكرر)** → يُعلَّم `duplicate`، يرجع `{alreadyRegistered: true}`.
   - **فشل حقيقي آخر (401 تهيئة/5xx بعد استنفاد المحاولات)** → يُعلَّم
     `manual_review`، **يُسجَّل بوضوح بـ`console.error`**، يرجع
     `{provisioned: false}` (المستخدم يرى شاشة "تم استلام طلبك" — شبكة أمان،
     وليست الحالة الطبيعية المتوقعة).
3. عند `provisioned: true`، الواجهة تنتقل لخطوة إدخال رمز OTP (6 أرقام).
4. `POST /api/trial-signup/verify` → يستدعي `/auth/register/verify` بجانب
   spruvex-r. عند النجاح يرسل بريد ترحيب (يحمل `dashboardUrl`)، ثم المتصفح
   يُحوَّل مباشرة عبر `window.location.href = dashboardUrl`.
5. `dashboardUrl` يأتي من spruvex-r نفسه (`dashboardUrl()` helper هناك) —
   حاليًا رابط عام للوحة التحكم، وليس رابطًا يحمل جلسة/token جاهزة.
   ⚠️ **معروف/ناقص**: لا صفحة "دخول تلقائي بجلسة جاهزة" — المستخدم يصل للوحة
   ويحتاج تسجيل الدخول يدويًا (بالبريد + **كلمة المرور التي اختارها بالفورم**).

### 4.4 كلمة المرور — تفصيل مهم

- سابقًا: كانت تُولَّد كلمة مرور عشوائية مجهولة (`crypto.randomBytes`) لا يعرفها
  أحد — كانت مشكلة تصميم فعلية (لا طريقة عادية لتسجيل الدخول لاحقًا).
- الآن: المستخدم يختار كلمة مروره في `/contact` (8+ أحرف، حرف + رقم على
  الأقل — **نفس** `PASSWORD_RULE` المستخدمة تمامًا في `RegisterDto` بجانب
  spruvex-r، معرّفة مرتين حرفيًا بنفس القيمة في كلا المشروعين، وليست منطقًا
  موازيًا مختلفًا).
- تصل لـ spruvex-r ضمن body الطلب لـ`/public/trial-signup`، وتُجزّأ
  (`hashPassword`) وتُستخدم مباشرة بدل التوليد العشوائي.
- **لا تُخزَّن ولا تُسجَّل بهذا المستودع (spruvex-site) أبدًا**، حتى مؤقتًا —
  لا بقاعدة SQLite المحلية ولا بأي `console.log`.

### 4.5 نوع النشاط (`businessType`)

- قائمة القيم: `restaurant | cafe | food_truck | dessert_cafe | other`
  (معرّفة في `BUSINESS_TYPES` بـ `constants.ts`، وبـ`PublicTrialSignupDto`
  بجانب spruvex-r بنفس القيم).
- يُخزَّن محليًا بعمود `trial_signups.business_type` **وأيضًا** يُمرَّر لـ
  spruvex-r ليُخزَّن بعمود `Tenant.type` هناك (عبر `provisionTenant()` الذي
  يقبله أصلًا) — تحقّقت فعليًا أن القيمة تصل وتُخزَّن بجدول `tenants`.

### 4.6 متغيرات البيئة — من جانب أي مشروع

| المتغير | بجانب spruvex-site | بجانب spruvex-r | ملاحظة |
|---|---|---|---|
| `SPRUVEX_R_API_URL` | ✅ (عنوان spruvex-r) | — | بدون `/api/v1` بالنهاية |
| `SPRUVEX_SITE_API_KEY` | ✅ | ✅ | **يجب أن تتطابق القيمتان حرفيًا** — لا مزامنة تلقائية. عدم التطابق = 401 صامت = كل تسجيل يسقط لـ"مراجعة يدوية" (هذا كان السبب الجذري لعطل حقيقي واجهناه — راجع §7) |
| `RESEND_API_KEY` | ✅ (بريد ترحيب/تنبيه إدارة) | ✅ (بريد OTP الحقيقي — منفصل تمامًا) | **يجب ضبطه على المشروعين كليهما بشكل مستقل** لتفعيل كل البريد |
| `RESEND_FROM_EMAIL` | ✅ | — | يجب أن يكون نطاقًا مُتحقَّقًا منه فعليًا بحساب Resend |
| `ADMIN_ALERT_EMAIL` | ✅ | — | وجهة تنبيه "تسجيل جديد" |
| `ADMIN_PASSWORD` / `ADMIN_SESSION_SECRET` | ✅ | — | خاصة بلوحة `/admin` بهذا الموقع فقط |
| `DATA_DIR` | ✅ (اختياري) | — | مسار SQLite (§5) |

راجع `.env.example` بهذا المستودع للنسخة الكاملة مع التعليقات، و`render.yaml`
(بجذر المستودع) لقائمة متغيرات Render اليدوية (`sync: false`).

---

## 5) قاعدة البيانات

### 5.1 هذا الموقع (spruvex-site) — SQLite محلي

⚠️ **ملف SQLite محلي على القرص، وليس Postgres.** يعمل فقط على استضافة Node.js
دائمة (Render Web Service + Persistent Disk، أو VPS/Docker) — **لا يعمل على
Vercel أو أي Serverless عديم الحالة**. هذا كان قرار النشر الأساسي وراء اختيار
Render بدل Vercel.

- **الموقع**: `{DATA_DIR}/spruvex-site.db` — `DATA_DIR` افتراضيًا `data/`
  بجذر المشروع محليًا، ويجب ضبطه على Render ليطابق `mountPath` الخاص بالقرص
  الدائم (`/var/data` في `render.yaml` الحالي) **بالضبط**.
- **الاتصال**: `src/lib/db.ts` — اتصال singleton (`getDb()`)، يُنشأ ويُهيَّأ
  فقط عند أول استدعاء فعلي من داخل route handler (ليس عند استيراد الملف —
  انظر التعليق بأعلى `db.ts` عن لماذا هذا مهم لبناء Next.js).
- **الجداول**:
  - `trial_signups(id, restaurant_name, phone, email, status, tenant_id,
    dashboard_url, business_type, created_at)` — `status` ∈
    `new | provisioned | manual_review | duplicate`.
  - `uploaded_files(id, original_name, mime_type, size_bytes, stored_filename, created_at)`
    — إيصالات التحويل البنكي، خارج `public/`.
  - `payment_submissions(id, restaurant_name, phone, plan_id, billing_cycle,
    amount_halalas, payment_method, transfer_reference, provider_ref,
    receipt_file_id, discount_code, status, admin_notes, created_at,
    reviewed_at, reviewed_by)` — `status` ∈ `pending | approved | rejected`.
    `amount_halalas` محسوب دائمًا من جانب السيرفر (`priceForCycle` +
    `applyPromoDiscount`)، **لا يُعتمَد على أي مبلغ من المتصفح أبدًا**.
- **جاهزية استبدال مستقبلي بـ Postgres**: هذه الطبقة مصممة عمدًا كـ repository
  functions (`src/lib/repositories/*.ts`) — استبدال `db.ts` باتصال Postgres
  (مثلًا Prisma) لا يتطلب تغيير أي صفحة أو route handler يستدعيها.

### 5.2 spruvex-r — Postgres (مستودع منفصل، خارج نطاق هذا المشروع)

- منتج SpruVex R الفعلي (NestJS + Prisma) يستخدم **PostgreSQL حقيقيًا**،
  مستودع Git منفصل تمامًا (`spruvex-r`)، **لا صلة له بملف SQLite أعلاه**.
- هذا الموقع لا يتصل بقاعدة بيانات spruvex-r أبدًا مباشرة — فقط عبر HTTP API
  كما بالقسم 4.
- الجداول ذات الصلة هناك (للعلم فقط، ليست جزءًا من هذا المستودع):
  `User` (email/phone/passwordHash)، `Tenant` (`type` = نوع النشاط)،
  `Subscription`، `OtpCode`، وغيرها. تفاصيلها الكاملة في مستودع `spruvex-r`
  نفسه (`apps/api/prisma/schema.prisma`).

---

## 6) البريد الإلكتروني (Resend)

ثلاث رسائل، من نظامين مختلفين تمامًا — **هذا فرق مهم يسبّب التباسًا بسهولة**:

1. **بريد "رمز التحقق" (OTP)** — **لا يُرسل من هذا المستودع إطلاقًا**. يُرسله
   spruvex-r نفسه عبر `ResendOtpSender` (موجود جاهزًا هناك بالفعل)، ويُفعَّل
   تلقائيًا **فقط** عندما تكون `RESEND_API_KEY` مضبوطة على **خدمة spruvex-r
   نفسها** (`identity.module.ts`: `useClass: process.env.RESEND_API_KEY ?
   ResendOtpSender : DevOtpSender`). بدونها، الرمز يُطبع فقط بسجلات
   spruvex-r (`[dev] OTP for ... : 123456`) ولن يصل أي مستخدم حقيقي أبدًا.
2. **بريد الترحيب** (بعد تحقق OTP ناجح، يحمل `dashboardUrl`) — يُرسل من هذا
   الموقع (`src/lib/email.ts` → `sendWelcomeEmail()`)، من
   `src/app/api/trial-signup/verify/route.ts`.
3. **بريد تنبيه الإدارة** (عند أي محاولة تسجيل، أي نتيجة) — من هذا الموقع
   أيضًا (`sendAdminSignupAlertEmail()`)، من `src/app/api/trial-signup/route.ts`.

كل دوال `email.ts` **best-effort**: فشل الإرسال (مفتاح غير صالح، نطاق `from`
غير مُتحقَّق منه، خطأ شبكة) يُسجَّل بوضوح بـ`console.error` **ولا يُفشل أبدًا**
تجربة المستخدم أو تدفّق التسجيل/التحقق.

---

## 7) المشاكل غير المحلولة / تحتاج فعلًا بشريًا — اقرأ هذا قبل أي شيء آخر

### 7.1 ✅ محلول — كان حرجًا، عولج جذريًا (2026-09-02)

**كان العرض**: التسجيل على النشر الفعلي (Render) يعرض رسالة "تم استلام طلبك،
سيتفعل خلال ساعات" بدل الانتقال لخطوة OTP، ولا يصل أي بريد.

**أكّد العمل الفعلي عبر Render API أن التوقف كان له ثلاث طبقات جذرية متراكبة،
كلها عولجت**:

1. **`SPRUVEX_SITE_API_KEY` لم يكن مضبوطًا إطلاقًا** على خدمة spruvex-r بـRender
   (مضبوط على spruvex-site فقط) → كل نداء كان يُرجع 401 صامتًا. عولج: أُضيف
   بنفس القيمة الحرفية على spruvex-r-api عبر Render API، وتحقق القراءة بعدها.
2. **خدمة spruvex-r-api كانت تنشر فرعًا لا يحتوي نقطة التسجيل أصلًا** — فرع
   `claude/spruvex-r-implementation-fythx2` (لا يوجد به
   `POST /public/trial-signup`؛ هي موجودة فقط بفرع
   `claude/public-trial-signup-endpoint`). عولج: حُوِّل فرع النشر للخدمة إلى
   فرع الـendpoint (وهو superset نظيف — merge-base يساوي رأس فرع
   implementation تمامًا) وأُعيد النشر.
3. **قاعدة إنتاج Postgres لم يكن بها سكيما أصلًا** — لم تُشغَّل `prisma migrate
   deploy` يومًا على `spruvex_r_quin` (لا خطوة migrate بالـDockerfile ولا يدويًا)؛
   كل استدعاء يمسّ DB كان يرجع 500 عامًا (أُثبت بفحوصات حية: login=500،
   register=500، trial=500 بينما health=200). عولج: أُضيف bootstrap مُتحكَّم
   بعلم `RUN_DB_MIGRATE=true` بجانب السيرفر (`run-db-migrations.ts` بنفس نمط
   `bootstrapAppRoleIfRequested` الموجود) يشغّل `prisma migrate deploy` كدور
   admin ثم يزامن كتالوجات permissions/units/plans، ثم نُشر مرة وعُزل العلم.
4. **دور admin بلا BYPASSRLS على Render** — كل جداول tenant تستخدم `FORCE ROW
   LEVEL SECURITY`، وBYPASSRLS لا يمكن منحه على managed Postgres (لا superuser)،
   فكان توفير الـTenant يفشل برسالة "Could not finish creating the trial
   restaurant". عولج جذريًا بـmigration جديدة
   (`20260902030000_admin_rls_explicit_access`) تنشئ policy صريحة
   `admin_full_access TO spruvex_admin` على كل جدول يحمل سياسة tenant_isolation
   (اكتشاف ديناميكي من pg_policies) — مكافئ BYPASSRLS لكن قابل للتعبير SQL
   بمالك الجدول. العزل للـapp role لم يتغير (اختبارات rls-isolation ناجحة).

**النتيجة النهائية المتحقق منها فعليًا** (2026-09-02): `POST /public/trial-signup`
على الإنتاج يرجع **201 حقيقيًا** بـtenantId + slug + dashboardUrl صحيح
(`https://spruvex-r-dashboard.onrender.com`) + trialEndsAt بعد 14 يومًا، بدون
`devOtp` (أي أن ResendOtpSender مُفعّل يرسل بريدًا حقيقيًا). لاحظ أن هذا اختبار
HTTP حي مباشر؛ **يبقى اختبار المتصفح الكامل + فحص صندوق الوارد للمستخدم** (الرسائل
الثلاث: OTP/الترحيب/تنبيه الإدارة) — راجع §14 نقطة 4.

### 7.2 ✅ محلول — التعديل مكتوب ومُختبر وم**دفوع** لمستودع spruvex-r (2026-09-02)

دعم كلمة المرور الحقيقية (§4.4) ونوع النشاط (§4.5) **مدفوع فعليًا الآن** لفرع
`claude/public-trial-signup-endpoint` بمستودع spruvex-r (Commit
`f542fcb`، وهو الفرع الذي تنشر منه خدمة spruvex-r-api على Render الآن —
راجع §7.1):
- `apps/api/src/modules/site-integration/dto/site-public.dto.ts` — أضاف
  `password` (مطلوب، نفس PASSWORD_RULE حرفيًا) و`businessType` (اختياري،
  IsIn بنفس قائمة BUSINESS_TYPES بالموقع).
- `apps/api/src/modules/site-integration/site-public.service.ts` — يستخدم
  `input.password` بدل التوليد العشوائي، ويمرر `input.businessType` كـ`type`
  لـ`provisionTenant()`.
- `apps/api/test/integration/site-trial-signup.e2e.spec.ts` — اختبار جديد
  يثبت تسجيل الدخول لاحقًا بنفس كلمة المرور المُختارة + وصول businessType
  لعمود `Tenant.type`.

اختبار حقيقي: **240/240 ناجحة** (19 suite، شاملة الاختبار الجديد) + lint +
typecheck نظيفة، ثم **201 حي على الإنتاج** بنفس الحقول (راجع §7.1).

### 7.3 ✅ محلول عمليًا (2026-09-02) — إعدادات Resend مضبوطة على الخدمتين

المفتاح الحقيقي (`re_MSVHKuDm...`، نطاق `spruvex.com` مُتحقَّق منه) أصبح
**مضبوطًا فعليًا على خدمتَي Render كلتيهما** (تقرأ عبر Render API وتأكيدت):

- خدمة **spruvex-r-api**: `RESEND_API_KEY` + `RESEND_FROM_EMAIL=info@spruvex.com`
  موجودان → `ResendOtpSender` مُفعّل (الدليل الحي: استجابة trial-signup بلا
  `devOtp` — الرمز يُرسل بريدًا حقيقيًا لا يُطبع بالسجلات).
- خدمة **spruvex-site**: `RESEND_API_KEY` + `RESEND_FROM_EMAIL` موجودان،
  وأُضيفت `ADMIN_ALERT_EMAIL=tqstore276@gmail.com` (كانت مفقودة).
- أُضيفت أيضًا `DASHBOARD_BASE_URL=https://spruvex-r-dashboard.onrender.com`
  لخدمة spruvex-r-api (كانت غائبة فكان الـdashboardUrl يرجع localhost:5173
  الافتراضي).

⚠️ **المتبقي الوحيد**: فحص صندوق الوارد الفعلي (وصول الرسائل الثلاث) — راجع
§14 نقطة 4؛ يتطلب من المستخدم تسجيلًا من متصفح ومراجعة بريده.

### 7.4 🟢 معروف منذ فترة، غير حرج

لا صفحة "دخول تلقائي بجلسة جاهزة" بلوحة spruvex-r بعد التحقق من OTP —
`dashboardUrl` رابط عام، والمستخدم يحتاج تسجيل دخول يدوي (بالبريد وكلمة
المرور التي اختارها). تحسين لوحة spruvex-r لدعم استهلاك token/جلسة جاهزة
مباشرة بعد التحقق خطوة تالية جيدة، خارج نطاق هذا المستودع.

---

## 8) الأمان — نقاط مهمة يجب الحفاظ عليها

- CSRF: نمط double-submit عبر كوكي httpOnly (`proxy.ts` يولّده، الصفحات
  تحقنه بحقل مخفي، السيرفر يقارن بـ`crypto.timingSafeEqual`).
- Rate limiting بالذاكرة لكل مسار حساس (تسجيل/تحقق/إعادة إرسال/دفع) — قيد
  معروف: محدود بعملية واحدة (لا يعمل عبر عدة instances)، مقبول لأن SQLite
  نفسه يفرض instance واحدة أصلًا (§5.1).
- `SPRUVEX_R_API_URL` / `SPRUVEX_SITE_API_KEY` / `RESEND_API_KEY` وكل سرّ آخر:
  تُقرأ فقط داخل دوال (lazy)، **ليس بمستوى الوحدة (module scope)** — تفاديًا
  لأي فشل بناء إن غاب المتغير وقت "Collecting page data"، وتفاديًا لتسريبها
  للحزمة العميلة.
- كلمة المرور: لا تُخزَّن ولا تُسجَّل أبدًا بهذا المستودع (§4.4).
- ملفات الإيصالات: أسماء ملفات عشوائية (UUID)، خارج `public/`، تُخدَّم فقط
  لمستخدم إدارة موثّق عبر `/api/admin/receipt/[id]`.
- مبلغ الدفع (`amount_halalas`) محسوب دائمًا من جانب السيرفر — لا يُعتمَد على
  أي رقم يرسله المتصفح، حتى مع كود الخصم.

---

## 9) النشر (Render)

راجع `render.yaml` (بجذر المستودع، خارج `spruvex-site/`) و`.env.example` و
README.md لتفاصيل تشغيلية كاملة. ملخص سريع:
- خدمة Node واحدة، `rootDir: spruvex-site`، `plan: starter` (Persistent Disk
  يتطلب خطة مدفوعة).
- Persistent Disk مُركَّب على `/var/data` — يجب أن يطابق `DATA_DIR` بالضبط.
- **⚠️ غير مُتحقَّق منه فعليًا بحساب Render حقيقي** — هذه البيئة لا تملك وصولًا
  لشبكة render.com. راجع لوحة Render فعليًا للتأكد من كل تفصيل.

---

## 10) آخر التعديلات (بترتيب زمني تقريبي)

1. بناء الموقع كاملًا من الصفر (صفحات، تصميم، أمان).
2. ربط `/api/trial-signup` بـ spruvex-r فعليًا (OTP، fallback عند الفشل).
3. إعداد `render.yaml` + إصلاح فشلي نشر حقيقيين (dependencies، ثم module-load I/O).
4. إصلاح مضاعفة بادئة `+966` بتطبيع رقم الجوال.
5. **هذه الجولة**: تشخيص سبب سقوط التفعيل التلقائي (§7.1) + تكامل Resend
   (§6) + فحص تكرار محلي + إعادة محاولة تلقائية للأخطاء العابرة + كلمة مرور
   حقيقية بالتسجيل (§4.4، يحتاج §7.2) + نوع نشاط (§4.5) + إعادة هيكلة تصميم
   (أيقونات، عنوان متحرك، زر واتساب، أسعار 45/70/109 + كود خصم اليوم الوطني
   الوظيفي فعليًا).
6. **جولة 2026-09-02 (تشغيل الإنتاج فعليًا عبر Render API + اختبارات حية)**:
   - ضبط `SPRUVEX_SITE_API_KEY` على خدمة spruvex-r-api (لم يكن موجودًا
     أصلًا) + `ADMIN_ALERT_EMAIL` و`DASHBOARD_BASE_URL` بخدمتي الموقع/API.
   - تبديل فرع نشر spruvex-r-api إلى فرع الـendpoint الصحيح.
   - دفع §7.2 (كلمة مرور + businessType، 240/240) لفرع spruvex-r المنشور.
   - إضافة bootstrap سكيما مُتحكَّم (`RUN_DB_MIGRATE`) + migration سياسة
     admin RLS الصريحة (بديل BYPASSRLS على Render) — نفسها دُفعت ونُشرت.
   - إزالة ghost submodule `spruvex-app` من هذا المستودع (`5e898bc`).
   - إصلاح بروكسي nginx للوحة (508 loop ثم 404 URI) — اللوحة والـPOS/KDS/
     Ordering/Platform كلها كانت معطلة عبر `/api/` (ترويسة Host + مسار).
   - إصلاح Resend نهائيًا: المفتاح القديم كان لحساب آخر (spruvex.erp) ونطاقه
     غير مُتحقَّق — مفتاح جديد من حساب tqstore276 (النطاق فيه أخضر) مضبوط
     على الخدمتين؛ بريد OTP حي مؤكد (200 من Resend + تسليم فعلي للمستخدم).
7. **جولة 2026-09-02 (مسائية — إصلاحات جذرية لتجربة التسجيل كاملة)**:
   - **دخول تلقائي بعد OTP** (يغلق §7.4): نقطة `POST /auth/handoff` جديدة
     + جدول `handoff_tokens` (رمز عشوائي hashed لمرة واحدة، صلاحية 15 دقيقة،
     استهلاك ذري يمنع إعادة التشغيل) — الموقع يصدره بالتسجيل (كوكي httpOnly
     `spruvex_handoff`) وصفحة `/auth/callback` الجديدة باللوحة تستهلكه عبر
     hash الرابط وتحذفه فورًا، فيهبط التاجر داخل اللوحة **مسجّل دخوله**.
   - **تطبيع البريد** (`normalizeEmail`): نزع `+tag` قبل `@` بكل نقاط
     الدخول (تسجيل/دخول/تحقق/إعادة إرسال/نسيت كلمة المرور + الموقع التسويقي)
     — يسد ثغرة "بريد واحد = عدد لا نهائي من الحسابات/التجارب" (أبلغ عنها
     مالك المشروع بنفسه). اختبار حي: `+promo@` يُرفض 409 كحساب موجود.
   - **استرداد الحسابات العالقة**: التسجيل ببريد له حساب **غير موثّق وبلا
     Tenant** (محاولة معلّقة من الحقبة المعطلة) يتبنّاه ويكمل التجربة بدل
     409 المسدود؛ وإزالة الاختصار المحلي بالموقع الذي كان يحجب هذا المسار.
   - **إصلاح بيانات الإنتاج** (`RUN_DB_REPAIR_EMAILS` + `repair-legacy-emails`):
     حسابات ما قبل التطبيع كانت تخزّن `+tag` كحسابات منفصلة، وبعد التطبيع
     أصبح الحساب الموثّق محجوبًا بظله غير الموثّق على البريد الأساسي. أُعيد
     الترتيب: الموثّق يرث البريد الأساسي، الظلال تُقعد (retired+id@invalid.
     local + isActive=false + تحرير الجوال). **تأكد حيًا**: دخول tqstore276
     بمسؤول خاطئ صار 401 (كان 403 غير موثّق) — أي كلمة مرورك الصحيحة تدخل.
   - **بوابة الشروط والأحكام**: checkbox إلزامي بالفورم + `z.literal(true)`
     سيرفريًا (لا يتجاوزه سكربت) + صفحتا `/terms` و`/privacy` جديدتان.
   - **صفحة دخول اللوحة الذكية**: عند 403 "بريد غير موثّق" تعرض تأكيدًا
     داخليًا (إدخال الرمز/إعادة إرساله) بدل العدد المسدود، و`/forgot-password`
     موجودة أصلًا وتعمل (§15 محلولة تبين أنها مبنية بالفعل).
   - **تحمّل cold-start**: مهلة نداء spruvex-r إلى 20 ثانية + إعادة محاولة
     ثالثة (3 ثوانٍ) — الخطة المجانية للـAPI تسبَت ويسقط أول تسجيل بالتوقيت
     القديم بلا داعٍ (بقي الحل الجذري ترقية الخطة — قرار مالك المشروع).
   - **حالة النهاية المختبرة حيًا (المسار الكامل كما يسلكه المستخدم)**:
     صحوة → تسجيل (CSRF+T&C) → provisioned → كوكي handoff → verify
     (رمز خاطئ 400 برسالة عربية دقيقة؛ الصحيح من بريدك) → تبادل handoff عبر
     بروكسي اللوحة 200 → إعادة استخدام 401 → `auth/me` 200 بالجلسة.
   - **اختبارات spruvex-r**: 243/243 (19 suite) على فرع endpoint + 233/233
     على فرع implementation (بلا site-integration فيه أصلًا) + فحص إصلاح
     البيانات المحلي (seed بنفس مرض الإنتاج → PASS).

---

## 11) كيف تختبر محليًا (تدفّق كامل حقيقي)

يحتاج تشغيل **مشروعين** معًا (لا يمكن اختبار التسجيل حقيقيًا بهذا المستودع
وحده):

```bash
# 1) spruvex-r (postgres + redis يجب أن يعملا أولًا)
cd spruvex-r/apps/api && npm run dev   # يستمع افتراضيًا على PORT من .env (عادة 3000)

# 2) spruvex-site
cd spruvex-site && npm run dev         # Next.js، يختار منفذًا بديلًا لو 3000 محجوز

# .env.local لهذا الموقع يجب أن يطابق:
#   SPRUVEX_R_API_URL=http://<عنوان spruvex-r المحلي>
#   SPRUVEX_SITE_API_KEY=<نفس القيمة المضبوطة بـ apps/api/.env بجانب spruvex-r>
```

رمز OTP بالتطوير المحلي (`NODE_ENV=development` بجانب spruvex-r) يظهر مباشرة
بسجلات spruvex-r: `[dev] OTP for <email> (email_verification): 123456`.

---

## 12) ملفات مرجعية عن منتج SpruVex R الكامل

أُضيفت بـ`docs/reference/` (داخل هذا المستودع) بناءً على طلب صريح من مالك
المشروع: خطة المنتج/المعمارية الكاملة (PDF كبير 143 صفحة + نسخة Markdown
مُراجَعة بالكامل)، لقطات شاشة المنتج (PDF + ZIP — **مكرَّرة مع** الصور
المستخدَمة فعليًا بالفعل تحت `public/screens/`)، وشعار كامل الدقة. راجع
`docs/reference/INDEX.md` لوصف كل ملف ولماذا لم تُراجَع ملفات الـPDF بعمق
(لا أداة عرض PDF متاحة بالبيئة وقت إضافتها).

⚠️ هذه الملفات تصف **منتج spruvex-r نفسه** (نظام إدارة المطعم الكامل: POS،
KDS، منيو رقمي، مخزون، ...) — وليست جزءًا من نطاق عمل هذا الموقع التسويقي.
أُرفقت هنا لتكون مرجعًا جاهزًا لأي عمل مستقبلي يحتاج فهم المنتج الأشمل، أو
لبناء ميزات بهذا الموقع تعرض معلومات حقيقية عنه (مثل لقطات الشاشة المستخدمة
بالفعل بالصفحة الرئيسية).

---

## 13) طلب ميزة جديدة (غير مبنية بعد): لوحة تواصل/شراكات (CRM)

طلب مالك المشروع إضافة قسم "تواصل" شامل للوحة تحكمه، بخمسة أقسام فرعية. هذا
**لم يُبنَ بعد** — موثَّق هنا بالتفصيل فقط لأنه نطاق عمل كبير جدًا (فعليًا
نظام CRM/شراكات كامل) يستحق تخطيطًا صحيحًا قبل التنفيذ، وليس إضافة سريعة.

### ⚠️ قرار معماري يجب حسمه أولًا (لم يُحسَم بعد)

لوحة `/admin` الحالية بهذا الموقع (spruvex-site) خفيفة عمدًا: SQLite محلي،
تراجع فقط طلبات التجربة/الدفع المُنشأة من هذا الموقع. لكن:
- **قسم 1 (تواصل التجار)** يحتاج بيانات اشتراك/باقة **حقيقية** (حالة
  `trial/active/past_due/suspended`، الباقة الفعلية) — هذه البيانات **لا
  توجد** بجدول `trial_signups` المحلي هنا؛ هي فعليًا بجداول `Subscription`
  و`Tenant` بجانب **spruvex-r**. نقاط `GET /api/v1/platform/tenants` و
  `GET /api/v1/platform/subscriptions` موجودة بالفعل هناك (`PlatformController`)
  لكنها **محمية بمصادقة إدارة منصة (Platform Admin)**، وهي **مختلفة تمامًا**
  عن `SPRUVEX_SITE_API_KEY` المستخدَم حاليًا (ذاك للتسجيل العام فقط، ليس
  لقراءة بيانات كل المستأجرين). يحتاج القرار: هل تُبنى هذه اللوحة كامتداد
  لهذا الموقع (ويحتاج عندها مصادقة Platform Admin جديدة للاتصال بـspruvex-r)،
  أم أن مكانها الصحيح هو **لوحة الإدارة الأصلية المخطَّط لها أصلًا بمستودع
  spruvex-r نفسه** (`apps/platform` — "Platform Super Admin"، مذكورة صراحة
  بخطة المنتج الكاملة، §11 من `docs/reference/SpruVex-R-Product-Architecture-Plan.md`،
  كتطبيق منفصل مخصص تمامًا لهذا الغرض: "tenants oversight, support tools").
- **الأقسام 2، 3، 5 (شركات التوصيل/بوابات الدفع/التقسيط)** بيانات جديدة
  كليًا — لا جدول لها بأي من المشروعين حاليًا.
- **القسم 4 (المسوّقون بالعمولة)** نظام إحالة/عمولات كامل — **غير موجود
  إطلاقًا** بأي من المشروعين (لا جدول referrals، لا حساب عمولة، لا تتبّع
  اشتراكات-عبر-كود).

**التوصية**: اسأل مالك المشروع صراحة قبل البدء أي مسار يريد (امتداد لهذا
الموقع أم منصة إدارة منفصلة بجانب spruvex-r)، لأن هذا يحدد المكدس والمصادقة
والبنية بالكامل — قرار من هذا الحجم لا يُتَّخذ افتراضيًا.

### 13.1 قسم 1 — تواصل التجار

- جدول: بريد، معلومات التاجر، **حالة الاشتراك الحقيقية**، التاريخ، **نوع
  الباقة الحقيقية** (راجع الملاحظة المعمارية أعلاه — ليست متوفرة محليًا الآن).
- بجوار كل تاجر: زرّا "إرسال بريد" و"إرسال واتساب" (رابط `wa.me/<phone>`
  جاهز، مثل `WhatsAppButton.tsx` الموجود).
- تحديد الكل / تحديد حسب الحالة (checkboxes + فلتر).
- إرسال رسائل إعلانية/تسويقية جماعية للمُحدَّدين (بريد على الأقل؛ واتساب
  الجماعي يحتاج WhatsApp Business API — رابط `wa.me` يفتح محادثة واحدة فقط
  في كل مرة، لا يدعم الإرسال الجماعي الفعلي بدون تكامل رسمي منفصل — وضّح هذا
  القيد للمستخدم قبل الوعد بإرسال جماعي عبر واتساب).

### 13.2 قسم 2 — تواصل شركات التوصيل

- جدول: اسم الشركة + كل بريد تواصل معها. ⚠️ **لا تُخترَع/تُفترَض أسماء شركات
  أو بريد تواصل** — إما ابحث فعليًا (أداة WebSearch متاحة) وتحقّق من صحة كل
  بريد قبل إدراجه، أو اطلب القائمة من مالك المشروع مباشرة. بريد خاطئ لشركة
  حقيقية أسوأ من عدم وجود بيانات إطلاقًا.
- نموذج بريد جاهز (خلفية + شعار + بريد تواصل + رقم واتساب + معلومات عن
  البرنامج، ثنائي اللغة عربي/إنجليزي) + ملف تعريفي PDF مرفق.
- زر "إرسال جديد" أعلى الصفحة → نافذة: اسم الشركة (عربي)، اسم الشركة
  (إنجليزي)، 6 حقول بريد (كل بريد يُرسَل له منفصلًا).
- الشركات المُضافة سابقًا بالجدول: البريد الجاهز مُجهَّز مسبقًا، زر "إرسال"
  بجوار اسم الشركة يرسله مباشرة.

### 13.3 قسم 3 — تواصل شركات نقاط الدفع/بوابات الدفع

نفس آلية القسم 2 بالضبط، لكن بنص/صياغة مختلفة (خاصة ببوابات الدفع). نفس
تحذير عدم اختراع بيانات شركات حقيقية.

### 13.4 قسم 4 — تواصل المسوّقين بالعمولة

- جدول: بريد، جوال، تاريخ بداية التسجيل، عدد المشتركين عبر كود الإحالة،
  مدة الاشتراك، الباقة، قيمة العمولة، المبالغ المُحوَّلة، المتبقي.
- زرّا إرسال بريد/واتساب لكل مسوّق. تحديد الكل/تحديد جزئي من الأعلى، إرسال
  عروض/حملات جديدة للمُحدَّدين.
- ⚠️ يتطلب **نظام إحالة كامل جديد**: كود إحالة فريد لكل مسوّق، ربطه بكل
  تسجيل تجربة (يحتاج حقل جديد بـ`trial_signups` أو بجانب spruvex-r)، حساب
  عمولة (نسبة من ماذا؟ سعر الباقة؟ الاشتراك الفعلي بعد التحويل من تجربة
  لمدفوع؟)، ودورة تحويل/تسوية العمولات (يدوية عبر لوحة الإدارة على الأرجح،
  مشابهة لمراجعة التحويل البنكي الحالية). **يحتاج تصميم منتج فعلي قبل الكود**
  — كثير من الأسئلة التجارية غير محسومة (نسبة العمولة؟ هل تُحسَب على التجربة
  أم الاشتراك المدفوع فقط؟ حد أدنى للسحب؟).

### 13.5 قسم 5 — شركات الدفع بالتقسيط

Placeholder فقط حسب الطلب — "لاحقًا نجهزها". لا حاجة لبناء أي شيء الآن غير
مكان فارغ بالقائمة الجانبية بعنوان واضح.

### 13.6 ملاحظات عامة من الطلب

- "أضف أي خانات/أزرار/ميزات أخرى مناسبة" و"اجعل لوحتي بها كل الخصائص لإدارة
  التطبيق والموقع" — طلب مفتوح جدًا عمدًا؛ لا تفسّره كإذن لبناء ميزات عشوائية
  بلا تخطيط. الأولوية: أكمل §7 (المشاكل الحرجة غير المحلولة) أولًا، ثم ناقش
  نطاق هذه اللوحة مع مالك المشروع تحديدًا قبل البناء.

---

## 14) قائمة تحقّق تفعيل Resend — الحالة الحالية (محدّثة 2026-09-02)

1. ✅ **خدمة spruvex-site بـRender**: `RESEND_API_KEY` +
   `RESEND_FROM_EMAIL` + `ADMIN_ALERT_EMAIL` كلها مضبوطة فعليًا (تحقق قراءة
   مباشر عبر Render API) → بريدا "الترحيب" و"تنبيه الإدارة" مفعّلان كودًا
   وإعدادًا.
2. ✅ **خدمة spruvex-r-api بـRender**: `RESEND_API_KEY` +
   `RESEND_FROM_EMAIL` مضبوطان → `ResendOtpSender` مُفعّل (الدليل: استجابة
   التسجيل لا تحمل `devOtp`).
3. ✅ **الشرط الأساسي §7.1** محلول جذريًا (راجع القسم — أربع طبقات عولجت).
4. ⏳ **التحقق الفعلي الأخير (بقية البند الوحيد)**: سجّل تجربة مجانية حقيقية
   **من المتصفح** على `https://spruvex-site.onrender.com/contact`، وأكّد بريديًا
   وصول الرسائل الثلاث: (أ) رمز التحقق OTP من spruvex-r، (ب) بريد الترحيب بعد
   إدخال الرمز (يحمل رابط اللوحة)، (ج) تنبيه الإدارة على tqstore276@gmail.com.
   HTTP-level مُختبر ومؤكد؛ بقي فقط فحص الوارد الفعلي لأنه يحتاج متصفحًا وصندوق
   بريد — كلاهما عند المستخدم.

   **ملاحظات تشغيلية مهمة اكتُشفت أثناء العمل**:
   - `RESEND_FROM_EMAIL` على خدمة spruvex-r-api = `info@spruvex.com` (بدون
     اسم عرض) — يعمل، ولو أردت اسمًا معروضًا "SpruVex R" عدّل قيمتها إلى
     `SpruVex R <info@spruvex.com>`.
   - `DASHBOARD_BASE_URL` أُضيفت لخدمة spruvex-r-api (كانت غائبة — راجع §7.3).

---

## 15) ميزة ناقصة: استعادة كلمة المرور المنسية (Forgot Password)

بعد إضافة كلمة مرور حقيقية يختارها المستخدم بالتسجيل (§4.4)، ظهرت حاجة
واضحة لم تُبنَ بعد: لا توجد أي واجهة أو نقطة وسيطة بهذا الموقع لتغيير كلمة
مرور مَن نسيها.

**الخبر الجيد**: الـBackend **جاهز بالفعل** بجانب spruvex-r — لا حاجة لأي
منطق موازٍ، فقط وسيط (proxy) بنفس نمط `trial-signup/verify` الحالي:

- `POST /api/v1/auth/forgot-password` — body: `{email}`. عام (`@Public()`)،
  Rate limit 5/دقيقة، يرجع 200 دائمًا (لا يكشف وجود الحساب من عدمه — نفس مبدأ
  `resend-otp` الحالي). يُصدر رمز OTP بغرض `password_reset` (نفس `OtpService`
  و`OTP_SENDER` المُستخدَمين لرمز التحقق — نفس قيد تفعيل Resend بـ§14 نقطة 2
  ينطبق هنا أيضًا).
- `POST /api/v1/auth/reset-password` — body:
  `{email, code (6 أرقام), newPassword}`. عام، Rate limit 10/دقيقة، يرجع 204.
  `newPassword` يخضع لنفس `PASSWORD_RULE` المستخدمة بالتسجيل (§4.4).

**المطلوب ببساطة** (نفس نمط الملفات الموجودة بالضبط، لا تصميم جديد):
1. `src/lib/spruvexR.ts`: دالتان جديدتان `forgotPassword(email)` و
   `resetPassword({email, code, newPassword})` — بنفس نمط
   `verifySpruvexRTrialOtp`/`resendSpruvexRTrialOtp` الموجودتين تمامًا.
2. `src/app/api/auth/forgot-password/route.ts` و
   `src/app/api/auth/reset-password/route.ts` — وسيطان بنفس نمط
   `trial-signup/verify/route.ts` (CSRF + rate limit محلي + Zod schema).
3. `src/lib/validation.ts`: schemas جديدة (`forgotPasswordSchema`،
   `resetPasswordSchema` بـ`newPassword`/`confirmNewPassword` — أعد استخدام
   `passwordSchema` الموجودة بالفعل).
4. واجهة: رابط "نسيت كلمة المرور؟" — الأنسب مكانًا **بلوحة spruvex-r نفسها**
   (صفحة تسجيل الدخول هناك، وليس هذا الموقع التسويقي، لأن هذا الموقع لا
   يحتوي أصلًا أي صفحة "تسجيل دخول" — فقط تسجيل حساب جديد). ناقش مع مالك
   المشروع أين يجب أن تعيش هذه الصفحة فعليًا قبل البناء (احتمال أنها أصلًا
   خارج نطاق هذا المستودع بالكامل، وموجودة أو يجب أن تُبنى بجانب لوحة
   spruvex-r مباشرة — راجع أيضًا التنبيه المشابه بـ§7.4 عن غياب صفحة دخول
   بعد التحقق).

---

## 16) ملاحظات/بنود أخرى غير مكتملة (مُجمَّعة من كل الجلسات السابقة)

- **§7.1** — ✅ محلول جذريًا (أربع طبقات: مفتاح API مفقود، فرع نشر خاطئ،
  سكيما DB غير مُطبَّقة، BYPASSRLS غير متاح على Render → سياسة admin صريحة).
  راجع القسم للحالات التي قد تتكرر مستقبلًا.
- **§7.2** — ✅ محلول: مدفوع (`f542fcb`) ومُنشَر ومُختبر حيًا (201).
- **§7.3 / §14** — ✅ محلول جذريًا (مفتاح من حساب Resend الصحيح — القديم
  كان لحساب مختلف ونطاقه غير مُتحقَّق؛ النطاق spruvex.com الآن Verified في
  حساب tqstore276) + تأكيد تسليم فعلي بصندوق المستخدم.
- **§7.4** — ✅ محلول: دخول تلقائي كامل عبر handoff tokens (راجع §10 جولة 7).
- **§13** — لوحة تواصل/شراكات (CRM) بخمسة أقسام — **قرار المستخدم حُسم: تُبنى
  داخل منصة spruvex-r** (وليست امتدادًا لهذا الموقع). لم تُبنَ بعد؛ تحتاج
  تصميم منتج قبل كود (خصوصًا نظام عمولة المسوّقين).
- **§15** — ✅ تبين أنها **مبنية بالكامل أصلًا** بلوحة spruvex-r:
  `ForgotPasswordPage` + مسار `/forgot-password` يعملان مع Backend جاهز
  (`POST /auth/forgot-password` و`/auth/reset-password`). لا عمل متبقٍ.
- **تطبيع البريد و+tag** — ✅ محلول (جولة 7) بكل النقاط + إصلاح بيانات
  الإنتاج (retired للظلال). أي بريد Gmail واحد = حساب واحد فقط.
- **بوابة الشروط والأحكام** — ✅ مبنية (checkbox + سيرفر + صفحتا /terms
  و/privacy).
- **لا اختبارات E2E آلية** (Playwright) محفوظة داخل هذا المستودع — كل
  الاختبارات التي أثبتت هذا العمل كانت سكربتات مؤقتة بمجلد scratchpad الخاص
  بالجلسة، لم تُحفَظ بالمستودع. لو أردت اختبارات دائمة قابلة لإعادة التشغيل،
  هذا بند إضافة مستقبلي.
- **Rate limiting بالذاكرة فقط** (§8) — يعمل لكن لا يصمد أمام أكثر من عملية
  Node واحدة (غير مشكلة حاليًا لأن SQLite يفرض عملية واحدة أصلًا، لكن يصبح
  قيدًا لو انتقل المشروع لقاعدة بيانات تدعم عدة عمليات مستقبلًا).

### 16.1 حالة النشر الحالية (مهم — محدّث 2026-09-02)

- **خدمة spruvex-r-api** (Render، docker، free): تنشر الآن من فرع
  `claude/public-trial-signup-endpoint` بمستودع `TareqQadi3/spruvex-r`
  (autoDeploy=yes على الـcommit). رأس الفرع المدفوع: `796b30a` (يتضمن
  `f542fcb` كلمة المرور + businessType، و`c94a5ac` bootstrap السكيما، و
  `796b30a` سياسة admin RLS). فرع `claude/spruvex-r-implementation-fythx2`
  (الفرع السابق للنشر) هو أسلاف مباشر لهذا الفرع — لا تفرّع موازٍ.
- **خدمة spruvex-site** (Render، node، starter): تنشر من فرع
  `claude/spruvex-marketing-site-bo7xy7` بهذا المستودع (آخر دفعة `5e898bc`
  — إزالة ghost submodule `spruvex-app`، لا كود تطبيقي تغيّر).
- **متغيرات Render**: `SPRUVEX_SITE_API_KEY` متطابق حرفيًا على الخدمتين؛
  Resend مضبوط على الخدمتين؛ `ADMIN_ALERT_EMAIL` و`DASHBOARD_BASE_URL`
  أُضيفتا؛ علم `RUN_DB_MIGRATE` أُزيل بعد اكتمال تطبيق السكيما (لا تتركه
  مضبوطًا إلا لغرض محدد ثم أزلته).
- **DB الإنتاج**: `dpg-da952r9srm7s73b77n4g-a` / قاعدة `spruvex_r_quin` —
  السكيما كاملة (12 migration) + كتالوجات permissions/units/plans مزامَنة.
  دور `spruvex_admin` يحمل سياسة `admin_full_access` عبر كل جداول RLS
  (البديل الصريح لـBYPASSRLS على managed Postgres).
- **طريقة تشغيل migrations مستقبلًا**: عند إضافة migration جديدة بمستودع
  spruvex-r، اضبط `RUN_DB_MIGRATE=true` على خدمة spruvex-r-api ثم Deploy
  مرة، ثم أزل العلم وDeploy ثانية — نفس نمط `RUN_DB_BOOTSTRAP` الموثق
  بـdocs/RENDER_DEPLOY.md (يمكن تركه مضبوطًا بأمان — idempotent — لكن الأنظف
  إزالته).
